function OneAway(str, str1){
	// Deletion: if length is shorter by 1
	// Insertion: Length is greater by 1
	// Replaced: Length are same
	if(Math.abs(str.length - str1.length) > 1){
		return false
	}

	
}